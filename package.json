{
  "name": "@veemex/open-reload",
  "version": "0.0.1",
  "description": "Hot-reload MCP meta-plugin for OpenCode — watches plugin files and dynamically reloads tools at runtime",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run src/index.ts",
    "test": "bun test"
  },
  "keywords": [
    "opencode",
    "mcp",
    "hot-reload",
    "plugin",
    "meta-plugin"
  ],
  "author": "Veemex",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "@opencode-ai/sdk": "^1.2.6"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
